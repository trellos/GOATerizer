# Asset sources and provenance

Every image GOATerizer ships is recorded here: what it is, who made it, and
under what licence. `AGENTS.md` §11 requires this even where attribution is not
legally required.

Nothing is hotlinked at runtime. All art is stored under `public/assets/` and
served by this project.

---

## Rocky Ascent, and its three companions — placeholder art (CURRENT)

`scripts/generate-placeholder-art.mjs` generates the assets that don't yet
have real art from shared drawing functions in `scripts/lib/rocky-art.mjs`: a
destination cairn, a dust puff, and a tick glint. Rocky Ascent High, Rocky
Descent and Rocky Descent High reuse that same set rather than each getting
distinct art, because their own scenario files say to: each names
`"Placeholder source families may be reused from the normal version; final art
can distinguish the high version later"` (Rocky Descent's file states the
same intent for reusing Rocky Ascent's families). Re-running the generator
reproduces every file byte for byte.

Every asset id below follows `<kind>_<scenario id>[_suffix]` — swap
`rocky_ascent` for `rocky_ascent_high`, `rocky_descent`, or
`rocky_descent_high` (and the directory for the matching
`public/assets/scenarios/<dir>/`) to get any companion's table.

| Asset id | File | Slot |
|---|---|---|
| `prop_rocky_ascent_goal` | `public/assets/scenarios/rocky-ascent/prop_rocky_ascent_goal.png` | `destinationVisual` |
| `fx_rocky_ascent_dust` | `…/fx_rocky_ascent_dust.png` | `stepEffects[0]` (contact) |
| `fx_rocky_ascent_tick` | `…/fx_rocky_ascent_tick.png` | `stepEffects[1]` (accent) |

The goat pose cycle, the background and the foothold are **no longer** in
this generated set — see "shipped real art" below. Directories:
`rocky-ascent/`, `rocky-ascent-high/`, `rocky-descent/`,
`rocky-descent-high/` — three generated files each, twelve total, plus seven
real-art files each (twenty-eight more) from the section below.

**The background and the pose cycle are drawn; the rest is not.** The
timeline-actor work moved the goat onto the note bars and reduced the scenario
panel to a backdrop. The backdrop is `bg_*`, and the goat standing on the bars
is the same art the panel used to show, cycled one pose per successful note
exactly as before — both are real art now, not generated (see below). The
cairn, dust and tick have no route or panel to appear in and are bound but
never drawn; they stay because they are the canonical slots. So does the
foothold's *slot* — `waypointVisuals[]` is bound but undrawn too — even though
the file behind it changed; see below for why it shipped anyway.

- **Author:** original work for this repository.
- **Licence:** CC0 / public domain. No third-party rights are involved.
- **Source:** generated by `scripts/generate-placeholder-art.mjs`
  (`scripts/lib/rocky-art.mjs` holds the editable drawing functions — the
  goat body/leg drawing code (`goatPose`/`goatFinish`), the foothold
  (`step`) and the background (`background`) were removed from it as each
  switched to real art; see that file's history if you need any of them
  back).

## Rocky Ascent, and its three companions — shipped real art (CURRENT)

The climber pose cycle — `goat_rocky_ascent_advance_01..04` and
`goat_rocky_ascent_finish` (and the matching ids in the other three
directories) — now ships real third-party art instead of the generated
placeholder. Twenty files: five ids × four directories
(`rocky-ascent/`, `rocky-ascent-high/`, `rocky-descent/`,
`rocky-descent-high/`), byte-identical across all four directories (same as
the generated art it replaces — the four scenario files say to reuse one goat
across the family, and this keeps that true).

| Asset id | File | Slot |
|---|---|---|
| `goat_rocky_ascent_advance_01` | `public/assets/scenarios/rocky-ascent/goat_rocky_ascent_advance_01.png` | `climberPoses[]` |
| `goat_rocky_ascent_advance_02` | `…/goat_rocky_ascent_advance_02.png` | `climberPoses[]` |
| `goat_rocky_ascent_advance_03` | `…/goat_rocky_ascent_advance_03.png` | `climberPoses[]` |
| `goat_rocky_ascent_advance_04` | `…/goat_rocky_ascent_advance_04.png` | `climberPoses[]` |
| `goat_rocky_ascent_finish` | `…/goat_rocky_ascent_finish.png` | `finishPose` |

- **Title:** Mountain Goat Sprites
- **Author:** Sevarihk
- **Licence:** CC-BY 4.0. The source page's own copyright notice: *"You are
  also allowed to share and repost my assets (or edits thereof) as long as you
  credit me and provide a link that leads back here or to my homepage."*
  Attribution for this build: **"Mountain Goat Sprites" by Sevarihk
  (<https://opengameart.org/content/mountain-goat-sprites>), CC-BY 4.0.**
- **Source:** <https://opengameart.org/content/mountain-goat-sprites>
- **Verified:** 2026-09-02, on the page itself (licence badge, author byline,
  and the copyright notice quoted above) — confirming the same pack an earlier
  session on `claude/goat-frontman-crowd-art` already verified and shipped for
  Goat Frontman's crowd goat, from different frames of the same files.
- **Files used:** `bergschaf-laufanimation-f.png` (252×252, a 4×4 sheet of
  63×63 cells) — row 2 (0-indexed row 1), columns 1–4, a four-frame
  side-profile walk cycle, for `advance_01..04` in source order.
  `bergschaf-bocksprung-f.png` (same layout) — row 2, column 1, a goat reared
  up on its hind legs with front hooves raised, for `finish`. Both source
  frames face **left**; this scenario's climber has always faced **right**
  (`scripts/lib/rocky-art.mjs:44`), so every frame was mirrored — see Edits.
- **Edits:** for each frame — cropped to the raw 63×63 grid cell, trimmed to
  the opaque (alpha > 8) bounding box, mirrored horizontally to face right,
  then downsampled (canvas `imageSmoothingQuality: "high"`, not
  nearest-neighbour) to a small crisp size (26px tall for the four advance
  poses, 30px tall for the taller reared-up finish pose) — the renderer draws
  with `imageSmoothingEnabled = false`
  (`src/ui/timeline/timeline-view.ts:580`), so, as with Goat Frontman's shipped
  art, a detailed source has to be pre-downsampled to the pixel grid it should
  read at. No colour was changed; the cream/tan coat and horn colour are the
  source's own, and already read as alpine rather than tropical. Both edits
  are within what CC-BY permits ("edits thereof"). The renderer does not
  require a fixed pixel size or matching dimensions across poses — it draws
  each `climberPoses[]` frame at the same fixed on-screen height and derives
  width from that frame's own aspect ratio (`src/ui/timeline/actor-layer.ts:342-345`),
  so nothing needed to be forced to exactly 24×18.
- **Why this and not a recolour or a different pack:** this repository's own
  house style (`scripts/lib/rocky-art.mjs`) already draws a cream-fur,
  tan-horn, no-black-keyline quadruped goat — the same coat this pack ships —
  so no recolour was needed (contrast Goat Frontman, which *did* need one and
  rejected it as unworkable; see that branch's `ART_SOURCING_FINDINGS.md` §2).
  No other pack found in this session's search (§ below) had a usable
  quadruped side-profile goat at any licence.
- **In the shipped build:** CC-BY requires the attribution to travel with the
  work, not just live in this doc, so it is duplicated at `public/CREDITS.txt`.
- **Known gap this session did not close:** the four scenario JSON files
  (`docs/scenarios/rocky-ascent*/`, `docs/scenarios/rocky-descent*/`) still
  list `placeholderSourceId: "spring_goat_ram"` and the old
  `sourceSelection`/`runtimeNotes` text for these five asset ids per scenario.
  That's stale now that real art ships from a different source under a
  different licence, but this task was scoped to asset files and this doc, not
  scenario JSON — a future session should update those fields to point at
  `oga_mountain_goat` (below) and correct the licence note.

### `bg_rocky_ascent` — shipped real art (CURRENT)

One asset id per directory, `bg_rocky_ascent` (and its three companion ids),
now ships a composited real painted backdrop instead of the generated
gradient-and-ridge one. Four files, byte-identical across all four
directories — the scenario files' own `runtimeNotes` for this id already say
*"Same source art can support all four levels; level escalation comes from
waypoint authoring,"* so one shared image was always the intent, not a
shortcut taken here.

| Asset id | File | Slot |
|---|---|---|
| `bg_rocky_ascent` | `public/assets/scenarios/rocky-ascent/bg_rocky_ascent.png` | `background` |

- **Title:** FREE Pixel Art Mountains Tileset and Backgrounds
- **Author:** karsiori
- **Licence:** CC0, confirmed on the source page: *"CC0, you can use this
  asset pack however you want, in your commercial or non-commercial
  projects."*
- **Source:** <https://karsiori.itch.io/free-pixel-art-mountains-tileset-and-backgrounds>
- **Verified:** 2026-09-02, on the page itself, and again on the itch.io
  download page reached from it (same licence text, plus the file listing).
- **Downloaded:** this pack sits behind itch.io's "pay what you want, $0
  minimum" flow — clicking the store page's "Download Now" goes to a
  `/purchase` page, not a direct file link. Per this project's operating
  rules that's a download needing the user's explicit go-ahead before
  proceeding, which is what happened here: the user asked for the download
  flow to be solved and the file fetched. The actual claim is genuinely free —
  the `/purchase` page's own "No thanks, just take me to the downloads" link
  claims the pack at $0 with no account, login or payment details, and lands
  on a page with a plain `Download` button. No payment was made, no account
  was created, and no credentials were entered anywhere in this flow.
- **File used:** `Pixel Art Tiles and Backgrounds - Mountains.zip` →
  `Backgrounds/Mountains 1.png` through `Mountains 4.png` (768px wide, several
  hundred px tall each — a near-to-far parallax set, each layer's peak
  silhouette starting at the very top of its own canvas and a lighter fill
  colour the nearer the layer) and `Backgrounds/Red Sky.png` (768×288, a warm
  dusk-red gradient — there is also a `Sky.png`, a cooler teal daytime
  variant used on the pack's own tropical/forest cover art, which is why
  that cover art reads as green and palm-treed and is not what shipped).
- **Edits:** composited in Node with `pngjs` (temporary dev dependency, not
  added to this repository's `package.json`) — `Red Sky.png` as the base,
  then `Mountains 1.png` through `4.png` alpha-composited on top in that
  order (farthest/darkest first), each cropped to its own top 768×288 region
  (every layer's silhouette starts at row 0, so this keeps the peaks and
  enough fill beneath them without needing the layer's full, much taller
  canvas). No recolouring — the warm rust-to-maroon dusk palette already read
  as alpine, not tropical, once the correct sky variant was used. Final size
  768×288, opaque, no transparency.
- **Why this and not the intended "ascent corridor" composition:** the
  scenario file's `sourceSelection` for this id asks to "leave the
  center-right ascent corridor readable" — a deliberate low point in the
  ridgeline, which the *generated* placeholder had (a valley notch between two
  peaks) and this composite does not; its ridge is busy corner to corner. That
  instruction predates the timeline-actor rework: `scenario-backdrop.ts`'s own
  header comment records that the scenario panel used to be a diorama the
  route was drawn onto, and is now "one job left: be the place the minigame
  happens" — a full-frame mood backdrop, nothing more. Note readability no
  longer depends on the backdrop having a clear patch: `timeline-view.ts`
  draws a `#drawBandScrim()` behind the note lane specifically so notes stay
  legible over the scenario's own art regardless of what is under them (the
  code comment: *"a cyan note on a pale mountain is not a readable note"*).
  On that basis a busier, better-composed real backdrop was judged worth
  the loss of the now-largely-decorative corridor.
- **Not used:** the pack's tileset (120 32×32 tiles) and 31 decoration sprites
  (grass, flowers, a house, etc.) — none of the bound slots call for them.

### `prop_rocky_ascent_step` — shipped real art (CURRENT)

| Asset id | File | Slot |
|---|---|---|
| `prop_rocky_ascent_step` | `public/assets/scenarios/rocky-ascent/prop_rocky_ascent_step.png` | `waypointVisuals[]` |

- **Title:** FREE Pixel Art Rock Pile Pack
- **Author:** karsiori
- **Licence:** CC0, confirmed on the source page, same wording as the
  mountains pack above.
- **Source:** <https://karsiori.itch.io/pixel-art-rock-pile-pack>
- **Verified:** 2026-09-02, same download flow and same no-payment path as
  the mountains pack above.
- **File used:** `Pixel Art Rock Pile Pack.zip` →
  `Rock Pile - SILVER -/Rock Pile 1 - SILVER - small.PNG` (41×20, no
  transparent padding — already a tight crop). The pack ships 15 pile shapes
  × 2 sizes × 6 colours (azure, beige, mossy, orange, silver, white); silver
  was picked because its sampled fill colours (roughly `104,109,120` through
  `224,225,228`) sit right in this scenario's own `rock`/`rockLight`/
  `rockDark` palette (`122,122,132` / `162,162,172` / `78,78,88` in
  `scripts/lib/rocky-art.mjs`) — a neutral cool grey, not the warm beige the
  cover thumbnail implies for the pack as a whole.
- **Edits:** none — used as downloaded.
- **Why a rock pile fits `waypointVisuals[]` but not `destinationVisual`:** a
  wide, flat rubble pile is exactly what a foothold wants — something a goat's
  foot lands on. It is a poor fit for `prop_rocky_ascent_goal`, which the
  generated placeholder draws as a tall stacked cairn (a summit marker, taller
  than wide); none of the pack's 15 piles are cairn-shaped, they are all wider
  than tall. Rather than ship a shape that reads as "more rubble" where the
  design wants "you have arrived," `prop_rocky_ascent_goal` was left as the
  generated cairn — see that placeholder table above.
- **Currently invisible regardless:** `waypointVisuals[]` is bound but not
  drawn by any renderer found in this codebase (same status as
  `destinationVisual`, `stepEffects[]` — see the placeholder section above).
  Shipped anyway because the fit was good and the download was already done
  for the background; a future session wiring up foothold rendering does not
  need to source art for it first.

### Why the old placeholder-source claim didn't hold

`docs/scenarios/rocky-ascent/rocky_ascent.scenario.json` named four
third-party CC0 packs as the intended placeholder sources, including
`spring_goat_ram` for the goat pose cycle. A previous session recorded that
its own environment could not reach `itch.io` or `opengameart.org` at all, so
none of the four could be verified from there.

This session's environment reaches both hosts. Direct `curl` to each root
returned `200`, and every source page below was opened, read and (where
relevant) its actual sprite sheet inspected pixel-by-pixel — not judged from a
search snippet or a stated licence alone. `spring_goat_ram` specifically
**is not a quadruped goat**: opening `goat or ram_strip5.png` shows a bipedal,
armoured ram *warrior* — navy armour, heavy black keyline, holding an axe. Its
CC0 licence claim was correct; the art was simply wrong for this scenario. An
earlier session working on Goat Frontman independently opened the same file
for a different scenario and reached the identical conclusion (see that
branch's `ART_SOURCING_FINDINGS.md`) — this doc now records it here too so a
future session doesn't re-open that page a third time.

## Rocky Ascent — other named sources, verified 2026-09-02

The scenario file's remaining three named sources, plus the one that's now
shipped, checked this session:

| Source id | Title | Author | Licence (verified on page) | Source page | Verdict |
|---|---|---|---|---|---|
| `oga_mountain_goat` | Mountain Goat Sprites | Sevarihk | CC-BY 4.0 | https://opengameart.org/content/mountain-goat-sprites | **Shipped** — see above; supersedes `spring_goat_ram` for the pose cycle |
| `spring_goat_ram` | Pixel Art Enemies — Axe Throwing Goat or Ram… | Spring Spring | CC0 option confirmed available | https://opengameart.org/content/pixel-art-enemies-axe-throwing-goat-or-ram-axe-throwing-monkey-or-ape-walking-pig | Rejected on fit, not licence — bipedal armoured ram warrior, not a quadruped goat. Superseded above |
| `karsiori_mountains` | FREE Pixel Art Mountains Tileset and Backgrounds | karsiori | CC0, confirmed on page ("you can use it however you want... no credit required though appreciated") | https://karsiori.itch.io/free-pixel-art-mountains-tileset-and-backgrounds | **Shipped** — see `bg_rocky_ascent` above. (The storefront's cover art looks green and tropical because it showcases the pack's *other* sky variant and decoration sprites, not the warm-dusk `Red Sky.png` composition actually used.) |
| `karsiori_rock_piles` | FREE Pixel Art Rock Pile Pack | karsiori | CC0, confirmed on page, same wording as above | https://karsiori.itch.io/pixel-art-rock-pile-pack | **Shipped for `prop_rocky_ascent_step`** — see above. Not shipped for `prop_rocky_ascent_goal`: none of the pack's 15 piles are cairn-shaped (all wider than tall), so the generated cairn stays. One provenance note for the record, not a licence problem: in this pack's own itch.io comments the author states they used AI image generation for their *storefront banner art* (not, by their account, the pixel-art packs themselves) — worth knowing, not disqualifying under the stated CC0 licence |
| `spring_dust` | Dust Animation | Spring Spring | CC0, confirmed on page | https://opengameart.org/content/dust-animation | **Not shipped.** Licence and file are fine — `dusty.gif` is a real, direct download (no purchase flow), CC0, tiny (10×7px per frame, grayscale). But `fx_rocky_ascent_dust` is one of the slots this file already notes as bound-but-never-drawn, so shipping it now would have zero visible effect; left as the generated placeholder. Would need a warm-tan recolour (grayscale source vs. this scenario's `dust` palette) if it's ever wired up |
| `codemanu_pixel_fx` | Free Pixel Effects Pack | CodeManu | CC0 / public domain, confirmed on page | https://opengameart.org/content/free-pixel-effects-pack | **Rejected on fit**, independently re-confirmed this session (matches the Goat Frontman session's finding for the same pack). The pack's own cover image shows large, multi-colour, 100×100px animated bursts — fire, ice-shatter, a poison-green ring — not the small clean single-shape glint `fx_rocky_ascent_tick` wants, and (like `fx_rocky_ascent_dust`) that slot is currently unbound and never drawn regardless |

### How to swap in what's left

For `prop_rocky_ascent_goal`, `fx_rocky_ascent_dust` and
`fx_rocky_ascent_tick` — no source found so far fits (see the table above):

1. Get the user's go-ahead before downloading anything from an itch.io
   `/purchase`-style page, even a nominally free one.
2. Download from the source page and re-verify the licence.
3. Extract the single frame(s) needed — the runtime treats every sprite
   as a **static billboard**, so strips, sheets and GIFs must be reduced to
   one frame each.
4. Save each as `public/assets/scenarios/<scenario dir>/<asset id>.png`,
   overwriting the generated placeholder — once per directory
   (`rocky-ascent/`, `rocky-ascent-high/`, `rocky-descent/`,
   `rocky-descent-high/`) you want to carry real art, using that directory's
   own asset ids.
5. Remove the corresponding entry from `scripts/generate-placeholder-art.mjs`'s
   `FAMILIES` loop (and the now-dead drawing function from
   `scripts/lib/rocky-art.mjs`, as was done for the goat, background and
   foothold) so a future generator run can't silently overwrite the shipped
   file.
6. Move that source's row into a shipped section of this file, with the
   licence you verified and the date you verified it.

No code change is needed: the asset ids and their slot bindings are scenario
data, and the runtime loads whatever is at those paths.

---

## Can Crushing — placeholder art (CURRENT)

The one `RepeatMinigame` scenario, and the only one outside the Rocky family. It
shares the generator and the discipline but none of the drawing functions: it is
a different class, a different theme and a different palette — beach at golden
hour rather than alpine dusk — so its shapes live in
`scripts/lib/crusher-art.mjs`.

Seven files for the seven slots the runtime binds. The ids are the canonical
ones from `GOATerizer_Scenario_Asset_Slot_Bindings.md` §3, so real art drops in
against the same names.

| Asset id | File | Slot |
|---|---|---|
| `bg_can_crushing` | `public/assets/scenarios/can-crushing/bg_can_crushing.png` | `background` |
| `hero80_can_crushing_ready` | `…/hero80_can_crushing_ready.png` | `performerNeutral` |
| `hero80_can_crushing_action` | `…/hero80_can_crushing_action.png` | `performerAction` |
| `hero80_can_crushing_finish` | `…/hero80_can_crushing_finish.png` | `performerFinish` |
| `prop_can_crushing_intact` | `…/prop_can_crushing_intact.png` | `repeatTarget` |
| `prop_can_crushing_done` | `…/prop_can_crushing_done.png` | `targetCompletedState` |
| `fx_can_crushing_impact` | `…/fx_can_crushing_impact.png` | `impactEffects[0]` |

The catalogue also names `debrisEffects[]` and `streakEffects[]` for this
scenario. The runtime's `RepeatAssetBindings` does not bind them yet, so no file
is generated for either — an unbound slot with no art is honest; an unbound slot
with art nothing loads is clutter.

- **Author:** original work for this repository.
- **Licence:** CC0 / public domain. No third-party rights are involved.
- **Source:** generated by `scripts/generate-placeholder-art.mjs`
  (`scripts/lib/crusher-art.mjs` holds the editable drawing functions).

**Note on the prototype.** The timeline-actor draft draws the crusher, his cans
and his pile with canvas primitives in `src/ui/timeline/repeat-layer.ts` rather
than from these PNGs, so only `bg_can_crushing` is drawn today. The Rocky family
no longer works that way — its goat is the real `climberPoses[]` art — and this
scenario should follow: `hero80_can_crushing_ready` / `_action` / `_finish` map
one-to-one onto the three poses the layer already switches between, and
`prop_can_crushing_intact` / `_done` onto the two can states.

The background is 384x216, because a backdrop now covers the whole frame
rather than the middle third of it — the same reasoning that gave Rocky
Ascent's own generated backdrop that size before it switched to a
768×288 real-art composite (see above); the runtime cover-fit-scales either
size to the actual panel regardless.

---

## Goat Frontman — shipped real art (CURRENT)

Two of the sixteen `PerformMinigame` assets are real, verified third-party
art. The other fourteen (below) remain the generated CC0 placeholder.

| Asset id | File | Slot |
|---|---|---|
| `react_goat_frontman_neutral` | `public/assets/scenarios/goat-frontman/react_goat_frontman_neutral.png` | `audienceStates[0]` |
| `react_goat_frontman_impressed` | `…/react_goat_frontman_impressed.png` | `audienceStates[1]` |

- **Title:** Mountain Goat Sprites
- **Author:** Sevarihk
- **Licence:** CC-BY 4.0. The source page's own copyright notice: *"You are
  also allowed to share and repost my assets (or edits thereof) as long as you
  credit me and provide a link that leads back here or to my homepage."*
  Attribution for this build: **"Mountain Goat Sprites" by Sevarihk
  (<https://opengameart.org/content/mountain-goat-sprites>), CC-BY 4.0.**
- **Source:** <https://opengameart.org/content/mountain-goat-sprites>
- **Verified:** 2026-09-02, on the page itself (licence badge, author byline,
  and the copyright notice quoted above).
- **Files used:** `bergschaf-lookup-f.png` (row 2, frame 1 — a standing/alert
  frame, all four legs grounded) for `_neutral`; `bergschaf-bocksprung-f.png`
  (row 2, frame 1 — reared up on its hind legs, front hooves raised, head
  thrown back) for `_impressed`. Both frames already faced left in the source
  sheet, matching this scenario's established crowd-goat convention, so
  neither needed mirroring.
- **Edits:** cropped to the frame's opaque bounding box, then downsampled with
  an alpha-weighted box filter (not nearest-neighbour) to a small, crisp size
  — the renderer draws with `imageSmoothingEnabled = false`
  (`src/ui/timeline/timeline-view.ts`), so a detailed painterly source has to
  be pre-downsampled to the pixel grid it should read at, or a naive
  nearest-neighbour downscale at render time turns it to noise. No colour was
  changed; the coat and horn colour are the source's own. Both edits are
  within what CC-BY permits ("edits thereof").
- **Why these and not a recolour to match the performer's black-coat/gold-horn
  palette:** tried and rejected — see `ART_SOURCING_FINDINGS.md` §2. The crowd
  ships in the source's own colouring.
- **In the shipped build:** CC-BY requires the attribution to travel with the
  work, not just live in this doc, so it is duplicated at `public/CREDITS.txt`
  — everything under `public/` is served as static output, so that file ships
  at the built site's root regardless of which scenario a given build includes.

## Goat Frontman — placeholder art (CURRENT)

The remaining fourteen assets. The scenario shares the generator and the
discipline with the Rocky family and deliberately borrows the Rocky goat body
— recoloured to a black coat with gold horns — so the frontman is visibly the
same animal; everything else (the stage, the mic stand, the light-bar note
art) is drawn in `scripts/lib/frontman-art.mjs`.

| Asset id | File | Slot |
|---|---|---|
| `bg_goat_frontman` | `public/assets/scenarios/goat-frontman/bg_goat_frontman.png` | `background` |
| `goat_goat_frontman_perform_01..04` | `…/goat_goat_frontman_perform_0N.png` | `performerPoses[]` |
| `goat_goat_frontman_bend` | `…/goat_goat_frontman_bend.png` | `flourishPoses[0]` |
| `goat_goat_frontman_slur` | `…/goat_goat_frontman_slur.png` | `flourishPoses[1]` |
| `goat_goat_frontman_finish` | `…/goat_goat_frontman_finish.png` | `finishPose` |
| `prop_goat_frontman_signature` | `…/prop_goat_frontman_signature.png` | `signatureProps[0]` (mic stand) |
| `fx_goat_frontman_swoosh` | `…/fx_goat_frontman_swoosh.png` | `flourishEffects[0]` |
| `fx_goat_frontman_sparkle` | `…/fx_goat_frontman_sparkle.png` | `accentEffects[0]` |
| `fx_goat_frontman_burst` | `…/fx_goat_frontman_burst.png` | `payoffEffects[0]` |
| `note_goat_frontman_light` | `…/note_goat_frontman_light.png` | `noteArt.body` |
| `note_goat_frontman_star` | `…/note_goat_frontman_star.png` | `noteArt.flourish` |

The crowd goat (shipped, above) is one sprite instantiated up to
`crowdCapacity` (24) times per attempt, varied only by position, scale and a
swap between its two states; that stayed true when it changed from generated
to real art.

- **Author:** original work for this repository.
- **Licence:** CC0 / public domain. No third-party rights are involved.
- **Source:** generated by `scripts/generate-placeholder-art.mjs`
  (`scripts/lib/frontman-art.mjs` holds the editable drawing functions — the
  crowd goat's drawing code was removed from it when the crowd shipped real
  art; see that file's history if you need it back).

## Goat Frontman — researched third-party sources, verified 2026-09-02

An environment that could reach the art hosts checked every row two earlier,
blocked sessions could only carry as an unverified claim (`ART_SOURCING_FINDINGS.md`
has the full write-up). Recorded here so the next session doesn't re-open the
same pages:

| Source id | Title | Author | Licence (verified on page) | Source page | Verdict |
|---|---|---|---|---|---|
| `oga_mountain_goat` | Mountain Goat Sprites | Sevarihk | CC-BY 4.0 | https://opengameart.org/content/mountain-goat-sprites | **Shipped** — see above |
| `spring_goat_ram` | Pixel Art Enemies — Axe Throwing Goat or Ram… | Spring Spring | CC0 option confirmed available | https://opengameart.org/content/pixel-art-enemies-axe-throwing-goat-or-ram-axe-throwing-monkey-or-ape-walking-pig | Rejected — the art is a bipedal armoured ram *warrior*, not a quadruped side-profile goat. Licence was never the problem; this asset also appears as an "intended source" in this file's Rocky Ascent section with the same problem |
| `oga_cc0_walk_cycles` | CC0 Walk Cycles (collection) | various | CC0 (collection filter) | https://opengameart.org/content/cc0-walk-cycles | Rejected — its goat/ram entry is `spring_goat_ram` above |
| `vectoraith_sideview` | Animated Sideview Sprite Pack — Normal Animals | VectoRaith | **Proprietary, paid** (min $2); licence explicitly forbids redistributing the files "or derivatives as is" | https://vectoraith.itch.io/animated-sideview-sprite-pack-normal-animals | Rejected — not redistributable into a public repo at any price |
| `seliel_livestock` | Pixel Art Creature Sprites — Livestock | Seliel the Shaper | Paid (min $19.99); not checked further | https://seliel-the-shaper.itch.io/livestock | Rejected — chicken, chick, cow, pig, duck; no goat |
| `codemanu_pixel_fx` | Free Pixel Effects Pack | CodeManu | CC0 | https://opengameart.org/content/free-pixel-effects-pack | Rejected on fit — multi-colour animated elemental VFX sheets (fire/ice/blood/poison), not the small clean glints this scenario's effect slots want |
| `spring_arena` | Welcome to The Arena! | Spring Spring | CC-BY family (3.0/4.0/SA variants) | https://opengameart.org/content/welcome-to-the-arena | Rejected — dark gladiator-colosseum, shot looking up at seating; wrong composition and wrong mood for `bg_goat_frontman` |

No stage/crowd/spotlight pixel-art pack with a usable licence turned up for
`bg_goat_frontman`; the generated backdrop stays. The mic stand prop and the
two note-art pieces were not actively re-searched — see
`ART_SOURCING_FINDINGS.md` §2 for why.

### How to swap the rest in

Same procedure as the Rocky family above: download, verify the licence, reduce
strips to single frames, save over the generated file under the same asset id,
and move the row into the shipped section with the verification date. If the
source's native resolution is much larger than the slot's placeholder (a
painterly sheet rather than hand-pixelled art), downsample it yourself with an
averaging filter before shipping — the renderer disables image smoothing, so a
high-resolution source scaled down at render time will look like noise rather
than crisp pixel art.

---

## Rules for any future asset

- Verify the source page and the licence before use; prefer CC0.
- Record source URL, author and licence here, even when attribution is not
  legally required.
- Store the file in this project; never hotlink at runtime.
- Do not introduce copyrighted commercial game assets as placeholders.
- Do not create many near-identical assets where a runtime transform or a
  duplicated instance produces the same result — one foothold sprite is
  instantiated up to thirty times per attempt, varied only by transform.
