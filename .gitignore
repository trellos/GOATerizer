node_modules/
dist/
.vite/
*.local

# Copied out of ../Tuninator/dist by vite.config.ts; never edited here.
public/assets/tuninator-worklet.js

# Regenerated by `npm run validate:browser` on every run.
validation-screenshots/

# Ad-hoc review screenshots and the throwaway scripts that make them.
# `npm run validate:browser -- --keep` is the reproducible source of truth.
ui-screenshots/
# Throwaway probe scripts kept at the root while investigating something.
.*.mjs
